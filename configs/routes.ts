const AppRoutes = {
    DEFAULT: '/',
    API_AUTH: '/api/auth',
    LOGIN: '/auth/login',
    REGISTER: '/auth/register',
    AUTH_ERROR: '/auth/error',
    SETTINGS: '/settings',
};

export default AppRoutes;
